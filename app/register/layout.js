export const metadata = {
  title: "FBA | Registration",
  description: "Register your team for Future Ballers Association tournaments and events",
};

export default function RegisterLayout({ children }) {
  return children;
}